#include<stdio.h>

#define max 10
int stack_arr1[max],stack_arr2[max];
int top2=-1,top=-1;
int isfull()
{
	if(top==MAX-1)
		return 1;
	else
		return 0;
}
int isEmpty()
{

	if(top==-1)
		return 1;
	else
		return 0;
}

void enque()
{
		
}
